# 深般若 — Graphical Heart Sutra Explorer

般若心経を「文字 → 句 → 語 → 構成要素」と際限なく潜っていける、グラフィカルな訳文兼辞書。
サイトの探索自体が瞑想体験になる UI を目指す。

> 本ドキュメントは `prompts/001.md`（企画）と `sample_image/*.png`（PC モック 3 枚）の分析結果をもとに、
> 実装に必要な情報をまとめたものである。経文本文・解説文本文は本ドキュメントには含めず、
> `content/` 配下のデータとして管理する（→ [コンテンツモデル](#コンテンツモデル)）。

## 目次

- [体験の骨格](#体験の骨格)
- [モック分析](#モック分析)
- [ルート画面（全文格子）](#ルート画面全文格子)
- [遷移演出](#遷移演出)
- [デザイントークン](#デザイントークン)
- [アーキテクチャ](#アーキテクチャ)
- [コンテンツモデル](#コンテンツモデル)
- [ナビゲーション](#ナビゲーション)
- [アセット在庫](#アセット在庫)
- [初期スコープ](#初期スコープ)
- [実装状況](#実装状況)
- [ディレクトリ構成](#ディレクトリ構成)
- [未決事項](#未決事項)

## 体験の骨格

1. 経文全体がほんわかと浮かんでレンダリングされ、各文字が微妙にゆらいでいる。
2. ある句に hover すると、その範囲の文字がフォーカスして明るくなる。
3. クリックで他の文字が退き、その句へズームイン。訳と解説が現れる。
4. 句の中の語に hover すると構成要素がふわりと周囲に並び、クリックでさらに一段潜る。
5. 以下、あらゆるレイヤー・あらゆる詳細度の用語で同じ操作が反復される。

全遷移はふんわりと滑らかに接続し、その間 BGM は途切れず鳴り続ける。

### 用語: 深度と性能ティアを混同しない

本ドキュメントでは 2 種類の「段階」が登場する。衝突を避けるため呼称を固定する。

| 呼称 | 意味 | 例 |
|---|---|---|
| **深度 `L0` / `L1` / `L2` …** | グラフを潜った階層。ユーザーの現在位置 | `L0` = 全文の紙面、`L1` = 句、`L2` = 句、`L3` = 語 |
| **性能ティア `Tier 1` / `Tier 2` / `Tier 3`** | 実行環境の描画能力 | `Tier 1` = WebGPU、`Tier 2` = WebGL2、`Tier 3` = WebGL 不可 |

「Tier」は性能の話にのみ使う。階層の話には使わない。

### 状態機械

ズーム中の多重クリックを殺すため、遷移相を明示的に持つ。

```
idle ──hover──▶ hovered ──click──▶ zooming-in ──▶ focused
  ▲                                                 │
  └──────────── zooming-out ◀──back / breadcrumb ────┘
```

`zooming-in` / `zooming-out` の間は入力を無効化する。`focused` からの子ノードへの遷移も同じ相を通す。

## モック分析

`sample_image/img_01.png` `img_02.png` `img_03.png`（いずれも 16:9 / PC 想定）から読み取った画面文法。

### 3 枚に共通するフレーム

縦書きの読み順に従い、**情報は右から左へ流れる**。右が「今いるノード」、左が「その詳説」。

| 位置 | 要素 | 備考 |
|---|---|---|
| 画面右端 | 現在ノードの大書（筆文字・縦組み） | 画面の主役。高さは画面の 60〜85% を占める |
| 大書のすぐ左脇 | 読み（かな）／梵語（カナ + ラテン翻字） | 極小サイズ・字間広め・縦組み |
| 右 1/4 付近 | 短い定義（サマリー） | 縦書き 2〜4 列、数十字 |
| 中央 | 子ノードの関係図 | ノードによって配置文法が変わる（後述） |
| 左 1/3〜1/2 | 長文解説 | 縦書き多列。列は右から左へ進む |
| 右上 | 「読み上げ」ボタン | ピル型の枠線 + スピーカーアイコン |
| 左下 | 左矢印 `←` | 「左にまだコンテンツがある」の示唆。細い白線の矢印のみ |

- 背景はほぼ完全な黒。要素間の余白が非常に大きく、密度は低い。
- 大書と中央の図は、**白い細い引き出し線**で結ばれる（img_03 で顕著。水平 → 段差 → 図の外周へ接続）。親子関係を視覚的に明示する装置。
- カーソル位置には通常のマウスポインタが表示されている（カスタムカーソルではない）。

### レイアウト文法は 3 種類ある

企画で言う `layout` ヒントは、モックから少なくとも次の 3 つが確認できる。

| ヒント | 出典 | 見た目 |
|---|---|---|
| `none` | img_01 | 子の図を持たず、大書とテキストのみ。句レベルのノード |
| `circle` | img_03 | 筆で描いた**円相**の内側に、子要素を円周上へ均等配置 |
| `column` | img_02 | **角丸矩形**のノードを縦に並べ、細い縦線で直列に連結 |

### img_01 — 句レベル（`layout: none`）

- 大書は 1 ノード分の語ではなく**句全体**を 2 列に折り返して大書する。
- 大書の左に読みガナが縦一列。さらに左に短い訳文、その訳文の下端に**淡いオレンジ寄りの細い縦罫**が入り、区切りとして働く。
- 中央〜左は解説の多列組版のみ。図は無い。

### img_03 — 円相レイアウト（`layout: circle`）

- 中央に、筆のかすれ・にじみを持つ**手描き感のある円**（正円ではなく、線幅が場所によって変わる）。直径は画面高の約 6 割。
- 円の内側に子 5 要素の筆文字を円周上へ配置。頂点付近から時計回りに 1 つずつ。中心は空。
- hover 中の 1 要素だけが**琥珀色に発光**し、他は白のまま。発光は文字の輪郭からにじみ出るソフトグロー。
- 円の中心は画面中央よりやや左（右端の大書ぶんオフセットされている）。

### img_02 — 縦連結レイアウト（`layout: column`）

- 角丸矩形（角丸半径は高さの約 1/4、いわゆるスタジアム形に近い）を 4 つ縦に等間隔配置。枠線は細い白、塗りは背景と同じ黒。
- 矩形どうしは**中央を通る 1 本の細い縦線**で連結。
- 最下段が hover 状態で、**文字と枠線の両方が琥珀色に発光**している（img_03 は文字のみの発光。枠を持つ図では枠も光らせる）。
- このノードでは大書からの引き出し線は見当たらない。`layout: column` では省略される、もしくは図の右上から伸びる仕様と解釈できる。

## ルート画面（全文格子）

モックに該当画面が無いため、ここで仕様を定める。

### メタファー: 写経の紙面

格子レイアウトの由来は**写経の紙面**である。この一点が以降の判断すべての根拠になる。

- 均質な升目に文字が整然と収まっている状態そのものが、第 1 層の絵になる。
- だから意味による分節をしない。写経の紙面に意味の区切りは書かれていない。
- だから紙面は画面に収まらない。**紙は画面より大きい**のが自然な状態である。

### 格子の組み方

- 全文を **16 文字で改行し続ける格子**として配置する。
- 意味による改行・分節は**行わない**。等間隔の格子を機械的に敷き詰める。
- 行数は文字数を 16 で割った値（データから導出。ハードコードしない）。最終行は端数のまま左寄せせず、行頭から詰める。
- 縦組みの読み順に従い、**列は右から左へ、文字は上から下へ**進む。1「行」= 縦 1 列。
- 字送り・行送りは等間隔。各文字は基準位置から微小にゆらぐ（→ ゆらぎは GPU 側）。

### 画面外へはみ出してよい

**PC・スマホの区別なく、格子は左へはみ出す前提で設計する。** ビューポートに収めるための縮小・折り返し変更・段組み変更は一切行わない。

- 字の大きさは**可読性から決める**（画面幅から逆算しない）。結果として紙面が画面より広くなるのは正しい状態。
- はみ出し方向は**左**。縦組みで列が右から左へ進むため、未読領域は常に左にある。
- 左下の `←` は「左にまだ紙面がある」ことの表示であり、モックの意図をそのまま満たす。
- 移動はドラッグによるパン（PC でもスマホでも同一の操作）。左右スクロールバーは出さない。
- 初期カメラ位置は**紙面の右上**、すなわち第 1 列の先頭。読み始めの位置に置く。
- パン可能な範囲は紙面の矩形にクランプする。無限に虚空へ流れていかないようにする。
- **紙面が余白ごと画面に収まってしまう画面では、右端ぞろえにせず中央ぞろえで固定する。** 送り先が無いのに右へ寄せると、
  左に大きな空白が空いて紙面が画面の隅へ追いやられて見える。はみ出す画面では従来どおり右端（第 1 列）から読み始める。
- 縦方向（1 列 16 文字ぶん）は**等倍では**画面高に収める。**はみ出すのは横方向のみ**とする。これにより「1 列は必ず読み切れる」状態を保つ。
- **拡大は等倍を下限とする補助操作。** ホイール／ピンチで等倍〜4 倍。拡大するとカーソル（ピンチなら 2 点の中点）の下の一点を固定したまま寄り、視野が狭まったぶん縦にも送れるようになる。等倍へ戻せば縦の可動域は消え、上の「1 列は必ず読み切れる」既定に戻る。
- 拡大率は L0 のみが持つ。潜ると等倍へ戻す（L1 以降は大書であって紙面ではない）。
- **L1 以降も左へはみ出す。** 狭い画面では本文（縦組みの解説）が画面の外へ出る。ここでも収めるための切り詰めや段組みの
  変更はせず、**同じドラッグで左右にだけ**送れるようにする。拡大も縦送りも持たない（大書の高さは画面高で決め打ってある）。
  送れる幅は本文・サマリーの左端の実測から引き、左端が画面に入り切ったところで止める。
- **パンの補間は 1 箇所だけに置く。** L1 以降のパンはばねを atom 側に持たせ、GPU レイヤーのカメラと DOM が
  同じ値を読む。カメラ側でも補間すると 2 度掛かって大書だけが本文に遅れ、2 つの層がずれて見える。

```
       ← 行の進行方向（右から左）

  行1  行2  行3  …
   ↓    ↓    ↓
  文字  文字  文字      ┐
  文字  文字  文字      │ 16 文字
  文字  文字  文字      │
   …    …    …        ┘
```

### 意味の区切りは L1 から

- **L0（ルート）では意味の区切りを一切見せない。** 格子は均質な紙面として提示する。
- 句としてのまとまりが現れるのは **L1 以降**。
- L0 での hover / フォーカスは、**一定範囲の文字だけが光る**表現とする。
  範囲は連続した文字インデックスなので、格子上では列をまたぐことがある。
  升目の途中で切れて次の列へ続く見え方をそのまま許容する（範囲を升目に合わせて調整はしない）。
- 光るのは文字のみ。升目の枠線や範囲を囲う図形は描かない。
- この「紙面は均質なのに、触れると意味の切れ目が浮かぶ」ギャップが L0 の体験の核になる。

### 句の範囲の持ち方

範囲は `kind: phrase` のノードが**全文に対する文字インデックス範囲**として持つ（→ [コンテンツモデル](#コンテンツモデル)）。
格子の座標は `index → (column, row)` の単純な写像で求まる。

列の切れ目は `content/sutra.txt` の**改行位置**に従う。1 行 = 1 列で、次の行は必ず新しい列の上端から始まる
（題名・「即説呪曰」・真言・「般若心経」がそれぞれ独立した列に立つ）。`COLS_PER_LINE = 16` は 1 列に収まる字数の
**上限**で、これを超える行だけが次の列へ折り返す。写像は `src/content/sutra.ts` の `GRID_CELLS` が持ち、
描画側（`Paper.tsx` / `PaperFallback.tsx`）は割り算をせずこの表を引く。逆引きは `indexAt(column, row)`。

## 遷移演出

深度をまたぐ演出。**潜るときと戻るときで非対称**にする。

### 持ち越される字 — 演出の軸

深度をまたぐとき、**薄れも現れもしない字がちょうど 1 ノードぶんだけある。** 選んだノードの字がそれで、
潜るときは「L0 で選んだ句 → 次の見出し」「図の中の子 → 次の大書」、戻るときはその逆をたどる。
この字だけが出発点から行き先へ動きながら描かれ、それ以外の字は（フォーカス範囲の内外を問わず）すべて出入りに乗る。

- 対応づけは**同じノードに属する字を同じ順に並べて 1 対 1** で取る。数が合わなければ持ち越しを諦め、全部が粒子になる。
- 持ち越しの字は紙面・大書の側では伏せられ（`carry.ts` の id と `materials.ts` の `carryOpacity`）、
  遷移の終わりに行き先の描画へそのまま引き継がれる。改めて現れさせない。
- したがって「生き残る字」は**フォーカス範囲の全字ではなく、選んだ 1 ノードの字**である。同一の存在として移動し続けるという
  要点は変わらないが、対象はこの粒度で確定した。

### 潜る（`L0 → L1`、以降も同様）

1. 選んだノードの字が明るさを保ったまま残る。
2. **それ以外の文字が粒子となって散開する。** 文字の形が解けて粒子群になり、外へ流れて消える。
3. 持ち越された字が、次の深度のレイアウト位置へ**連続的に移動・再配置**される。

文字のアイデンティティが保たれることで、潜っても同じ紙面の続きにいる感覚が維持される。

### 戻る（`L1 → L0`、以降も同様）

1. **今ある文字はそのまま残り、** 元のレイアウト位置へ連続的に再配置される（消さない）。
2. それと並行して、**粒子がフェードインしながら凝集し、周囲の文字を形作る。**
3. 行き先の字は**最初から濃さ 0 で置いてある。** 凝集と同じ時間で濃くなり、粒子が形に収まる時点で出そろう。
4. 粒子は**収まってから遅れて引く。** 光が字に重なったまま薄れるので、粒子とメッシュの切替が絵として見えない。

3 と 4 が要点で、「凝集しきってから字を出す」順にすると、そこで必ず間が空く。字は先に置いて薄いまま待たせ、
光のほうを後から引かせる。

散開の逆再生ではない。**散開は「解けて飛ぶ」、復帰は「無から浮かび上がる」** という別の運動にする。
戻る操作で紙面が虚空から現像されてくる感覚が、写経の紙面のメタファーに合う。

### 時間の設計

尺とカーブは 1 か所（`scene/Transition.tsx`）が持ち、粒子・字・カメラ・DOM がすべてそこから引く。

| 項目 | 値 / 方針 |
|---|---|
| 演出の尺 | `TRANSITION_MS = 1700` |
| 補間カーブ | 5 次（smootherstep）の `ease()`。両端で加速度も 0 になり、動き出しに角が立たない |
| カメラ | **持ち越しの字と同じ時計・同じカーブで動かす。** ずらすとワールドでは直線でも画面上では字が寄り道して見える |
| 字の出入り | 消えるのは 2 乗のカーブで素早く。現れるのは着地を見せきってから（`APPEAR_DELAY_MS = 500`） |
| 字の現れ（L0 へ戻るとき） | 例外。遅延を挟まず、進行度 `ENTER_FROM = 0.5` から尺いっぱいかけて濃くする。**粒子の凝集と同じ時間で立ち上げる** |
| 粒子の尺 | **字とは別に持つ。** 字がさっと引いたあと、残った粒子だけがふわりと流れていく順序を作る |
| 粒子の余韻 | 演出の**あと**に伸びる `PARTICLE_FADE_MS = 520`。凝集しきった光はここで引く。位置は home に着いたまま濃さだけを落とす |
| DOM オーバーレイ | 同じ遅延を引いて字に追随する |

### 実装メモ

| 項目 | 方針 |
|---|---|
| 粒子のホームポジション | ビルド時に前計算した粒子サンプル座標（→ [グリフの扱い](#グリフの扱い演出の核)） |
| 持ち越される字 | 粒子系ではなく**メッシュのまま**補間で移動させる。粒子化しない |
| 散開する文字 | メッシュを消し、同じ位置から粒子を発生させて外向きに流す |
| 散開の運動 | 上向きに偏らせる（真上は真下のちょうど 2 倍飛ぶ）。終わりぎわに浮き上がる等加速度 + 粒ごとにずれた横揺れ。**煙が立ちのぼる向き**を作る |
| 粒の形 | 円形（板の UV から抜く）。四角い粒は紙の上の塵に見えない |
| 復帰する文字 | 粒子を散開時とは別の分布（画面全体からの薄い流入）から凝集させ、収束後にメッシュへ差し替える |
| 差し替えの隠蔽 | 粒子 → メッシュの切替は、両者を重ねて隠す。字は凝集と同じ時間で立ち上げ、粒子は演出のあとに引く（→ [戻る](#戻るl1--l0以降も同様)） |
| 濃さのユニフォーム | **深度ごとに 1 本ずつ**（`paperOpacity` / `nodeOpacity`）。戻りぎわは大書と紙面が同時に画面へ居るので、1 本では片方を薄めながらもう片方を出せない |
| 面の用意 | **やり直さない**（→ 次節） |
| 描画経路 | **`Points` ではなく `Sprite` のインスタンシング。** `PointsNodeMaterial` の `sizeNode` は `Points` に対して読まれない（WebGPU の点は 1px 固定、WebGL2 経路は `material.size` を見る）ため、粒ごとの大きさが要るならこの経路しかない |
| 入力の無効化 | 演出中は `zooming-in` / `zooming-out` 相にあるため入力を受けない（→ [状態機械](#状態機械)） |

### 面の用意はやり直さない — 演出が固まる原因

**紙面（L0）は一度組んだら畳まない。** 潜っているあいだも置いたままにし、描かず・フレームの計算もせず・
当たり判定にも出さないだけにする（`Paper` の `live`）。マウントし直すと、90 字ぶんのマテリアルと描画オブジェクトの
用意をその 1 フレームで払うことになり、そこで固まる。**どのタイミングでマウントしても固まる場所が変わるだけ**で、
演出の終わりに置けば着地が跳ね、頭に置けば動き出しが跳ねる。捨てなければ three はシェーダのプログラムを
持ち続けるので、戻ってきても組み直しが起きない。

紙面のマテリアルは**字ごとではなく紙面で 1 本**にする。ムラの種・フォーカス量・持ち越しはすべてインスタンス属性で
持たせてあるので、シェーダそのものは字によらず同じ。沈み込み（`dim`）も 1 本で、進めるのは `Paper` 本体だけ
（字ごとに寄せると 1 フレームで 90 回進んでしまう）。

固まったり固まらなかったりする場合、疑うのは**マテリアルを作り直しているところ**である。TSL が吐くシェーダの
ソースには属性の通し番号が入るので、作り直すたびにソースが別物になり、ブラウザのプログラムキャッシュに
当たったり当たらなかったりする。確率的に見えるのはこれによる。

なお、シェーダの用意だけを別スレッドへ逃がすことはできない。WebGL / WebGPU のコンテキストはキャンバスを持つ
スレッドのものなので、`OffscreenCanvas` でレンダラごとワーカーへ移すしかなく、[2 レイヤー合成](#2-レイヤー合成)と
ポインタ入力を組み直すことになる。**先に作って捨てない**のが答えで、ほんとうに先読みが要るときの手は
読み込み中の `renderer.compileAsync(scene, camera)`。

### 性能ティアごとの落とし所

演出の**筋書きは全ティアで共通**にし、粒子数と表現の粒度だけを落とす。

| ティア | 散開・凝集の表現 |
|---|---|
| `Tier 1`（WebGPU） | 数十万粒子。コンピュートシェーダで 1 文字あたり数千点を個別に動かす |
| `Tier 2`（WebGL2） | 粒子数を 1 桁落とす。頂点シェーダの時間ベースアニメで散開・凝集を表現する。1 文字あたりの点数を減らし、密度の低さを不透明度とサイズで補う |
| `Tier 3`（WebGL なし） | 粒子表現なし。DOM のフェードイン／アウトのみ。文字の再配置は CSS transition で行う |

## デザイントークン

モックから抽出した値（近似値。実装時に微調整する前提）。

```css
:root {
  /* 面 */
  --bg:            #050505;  /* ほぼ黒。完全な #000 ではなく僅かに浮かせる */
  --vignette:      radial-gradient(ellipse at 50% 50%, #0a0a0a 0%, #000 85%);

  /* 文字 */
  --ink:           #f2f0ec;  /* 大書・筆文字。わずかに温かい白 */
  --text:          #d8d5cf;  /* 解説本文 */
  --text-dim:      #8e8a84;  /* 読みガナ・梵語・補助 */
  --rule:          #b98a4a;  /* img_01 の区切り罫。淡いオレンジ */

  /* 状態 */
  --focus:         #ffd08a;  /* hover / 選択の琥珀 */
  --focus-glow:    #ffb85c;  /* グローの芯 */

  /* 図形 */
  --stroke:        rgba(242,240,236,.55);  /* 円相・矩形枠・連結線 */
  --stroke-hover:  var(--focus);
}
```

- 彩度は全体で極端に低い。琥珀のアクセント以外に有彩色を出さない。
- グローは 2 段（近距離の強い滲み + 遠距離の弱い滲み）。DOM 側は `text-shadow`、WebGPU 側は
  ビルド時に焼いた**字ごとの符号付き距離場（SDF）**を引いて、輪郭からの距離で減衰させる（→ [グリフの扱い](#グリフの扱い演出の核)）。
  板の中心から放射させると字が違っても同じ丸い光になり、「字が光っている」ではなく「字の裏に電球がある」絵になる。
- 白の使い分けは 3 段（`--ink` > `--text` > `--text-dim`）。この階層が奥行きを作っている。

### 墨の質感

グリフの塗りは均一な白ではなく、**濃淡とかすれのある墨**にする。テクスチャは持たず全て手続き的に出す
（→ [ランタイムで SVG を触らない](#グリフの扱い演出の核)に抵触しない）。色は増やさず、既存トークンの明度と不透明度だけを揺らす。

- 成分は 3 つ。にじみ・墨だまりの大づかみな濃淡／紙の目／筆の流れに沿って引き伸ばしたかすれの筋。
- **模様の粗さはワールド単位で一定にする。** 字の大きさに比例させると、大書では筋が太くなりすぎて墨ではなく金属の光沢に見える。
  字面ローカル座標にその字のワールドでの大きさを掛けてから引く（座標は字と一緒に動くので、ゆらぎで模様が泳ぐこともない）。
- 種は字（インスタンス）ごとに振る。同じ字が紙面に何度現れても刷り物に見えない。
- 最も薄いところでも一定の濃さを残す。0 まで落とすと字が穴だらけになる。

### ゆらぎ

紙面（L0）も大書と図（L1 以降）も、遷移で持ち越される字も、**同じ規則で揺れる**。どの深度でも「同じ場に浮かんでいる」と
見えることが要点なので、規則は 1 か所（`scene/sway.ts`）だけが持つ。

- 振れ幅は**字の大きさに対する比**で持つ。絶対値にすると、大書のような大きい字では揺れが相対的に消えて固まって見える。
- 位相は字ごとにずらす（揃うと紙面が波打つ）。種は紙面が全文インデックス、大書と図が字のコードポイント。
  墨と滲みが別メッシュに分かれても、遷移で出発点と行き先に分かれても、同じ種から引き直せば揺れの位置ごと繋がる。
- x / y / 傾きで周期をわずかにずらし、単純な往復に見せない。

### タイポグラフィ

| 用途 | 書体 | 組み |
|---|---|---|
| 大書・図中の文字 | 筆文字（`assets/svg/` の 118 字を使用） | 縦組み。WebGPU レイヤーで描画 |
| 解説本文・サマリー | 明朝系（可読性優先） | `writing-mode: vertical-rl`。DOM レイヤー |
| 読み・梵語 | 同上・極小 | 縦組み・字間広め。ラテン翻字は縦中横にせず縦に流す |
| 現在位置インジケータ | ゴシック or 明朝（可読性優先、筆文字にしない） | 縦積み |

## アーキテクチャ

### 2 レイヤー合成

| レイヤー | 内容 | 技術 |
|---|---|---|
| WebGPU レイヤー（Canvas） | 大書の筆文字グリフ、粒子の散開・再凝集、ゆらぎ、hover 発光、円相・連結線の描線 | three.js `WebGPURenderer` + TSL、@react-three/fiber v9 |
| DOM レイヤー（HTML オーバーレイ） | 縦書きの長文解説、サマリー、読み・梵語、読み上げボタン、現在位置インジケータ、左矢印 | `writing-mode: vertical-rl` + motion |

数百字級の縦組みを WebGL で描くと品質・実装コスト・アクセシビリティのすべてで損をするため、
**組版は DOM、絵は GPU** に振り分ける。筆文字は SVG パスがあるので GPU 側の素材として最適。

**カメラのドラッグ移動は両レイヤーを同期させる。** ワールド座標系のコンテナを 1 つ定義し、
three.js のカメラと DOM オーバーレイの `transform: translate3d(...)` に同じ変換を適用する。
`drei` の `<Html>` ではなくワールド固定の独自オーバーレイにする（長文組版の制御性、およびフォールバックの都合）。

### `L1` 以降の寸法は 1 か所が持つ

大書と子の図の位置・大きさは `world/node-layout.ts` だけが持ち、描画（`NodeStage`）・遷移（`Transition`）・
DOM の組版（`Overlay`）が同じ関数を引く。**ずれると遷移の継ぎ目で字が跳ね、オーバーレイが大書に重なる。**
three に依存しない場所へ置くのはこのためである。

- **大書は列を上で揃える。** 長さの違う列が並んでも書き出しの高さが動かない。1 字の大きさはいちばん長い列から決めるので、
  句のように長い `label` でも必ず画面高に収まる。
- **横に狭い画面では、大書がはみ出すぶんだけカメラごと右へ送る**（`nodePanX`）。大書だけを左へ寄せるのではなく視野ごと動かすので、
  大書と図の間隔＝右から左への情報の流れは変わらない。収まる画面では 0 を返し、設計どおり図が画面中央に来る。
- 大書の右に残す余白は**ワールド比ではなく px** で持つ。字の大きさは画面高から決まるため、横に狭い画面ほど大書は相対的に大きくなり、
  比で持つと読み（かな）の帯が潰れる。ただし幅に対する上限も置く。

### DOM の位置決め — `overlayInsets`

読み・サマリー・本文は大書の実寸に合わせて置きたいが、大書は WebGPU レイヤーにあり DOM からは測れない。
そこで `node-layout.ts` がカメラと同じ式でワールド座標を画面座標へ直し、**画面右端からの距離（px）**を 3 つの目印として渡す。

| 目印 | 意味 |
|---|---|
| `headlineRight` | 大書の右端。ここより右が読み（かな）の帯 |
| `headlineLeft` | 大書の左端。ここより左がサマリーと本文 |
| `diagramLeft` | 図の左端。図を持たないノードでは大書の左端と同じ（本文への制約にならない） |

組版そのものは CSS の役目で、目印は CSS 変数として渡すだけ。間合いを変えるときは `.overlay` の
`--reading-gap` / `--summary-gap` / `--doc-gap` を触る。`reading` の空白は `label` と同じく**列の切れ目**として読む。

### 技術スタック

| 領域 | 採用 | 備考 |
|---|---|---|
| ビルド | Vite + TypeScript | SSR 不要。静的ホスティング（Cloudflare Pages 等） |
| 3D | three.js `WebGPURenderer` + TSL / r3f v9 | `gl: async (props) => { const r = new WebGPURenderer(props); await r.init(); return r }` |
| 状態 | Jotai | 現在ノード、遷移フェーズ、設定（音量等） |
| 遷移制御 | 小さな手書き FSM（or XState 軽量利用） | 上述の相を明示的に持つ |
| アニメーション | TSL（粒子）+ spring 補間（カメラ・DOM） | DOM 側は motion |
| コンテンツ | YAML（グラフ）+ Markdown（解説） | `import.meta.glob` + ビルド時パース、zod で検証 |
| 音声 | Web Audio API 直 | 1 トラックを常時ループ。階層による切替なし。初回はユーザー操作で開始 |

`drei` への依存はほぼ不要（WebGL 前提のコンポーネントが混ざるため避ける）。

### グリフの扱い（演出の核）

各文字について、**ビルド時に** SVG から 3 つの表現を生成しておく。ランタイムで SVG をサンプリングしないのがスマホ性能上の要点。

1. **メッシュ** — `SVGLoader` でパス → 三角形分割。通常表示・hover 発光用。フィルは TSL マテリアルで墨のかすれ・にじみを付与。
2. **粒子サンプル** — 塗り領域から数千点／字をサンプリングした座標配列（バイナリで同梱）。コンピュートシェーダの「ホームポジション」として使い、散開 ⇄ 再凝集・文字間モーフをすべて GPU 側で補間。
3. **符号付き距離場** — 字面より一回り広い正方セル（64×64 / 字）に、輪郭からの距離を焼いた 8bit のアトラス。hover 発光の滲みは、板の中心からの半径ではなくこの距離で減衰させる。**光が字の形から出ている**ように見えるかはここで決まる。

118 字 × 4,000 点 ≒ 45 万点。WebGPU コンピュートで十分回る規模。

### 性能ティア

| ティア | 条件 | 内容 |
|---|---|---|
| Tier 1 | `navigator.gpu` あり | フル演出。数十万粒子、コンピュートシェーダ |
| Tier 2 | WebGL2 のみ | 粒子数を 1 桁落とし、頂点シェーダの時間ベースアニメへ切替。中位機のスマホはここ |
| Tier 3 | WebGL 不可 | Canvas を一切マウントせず、DOM レイヤー + CSS フェードのみ |

- 判定順は `navigator.gpu` → WebGL2 コンテキスト取得試行。
- `devicePixelRatio` は 1.5〜2 で上限キャップ。
- 起動後数秒のフレームタイム計測で粒子数を動的に下げ、機種差を吸収する。
- Tier 3 は 2 レイヤー構成の帰結として「WebGPU レイヤーを外すだけ」で自然に得られる。

### ターゲット環境

- 最新のスマホ・タブレット・Chrome のみを対象とする。
- **スマホ対応は必須要件。** ただし左右スクロールではなく、**ドラッグでカメラのフレームを移動**させる操作にする。
- パン可能な方向は左矢印（`←`）の明滅で示す。

## コンテンツモデル

グラフ構造は YAML、解説文は Markdown + frontmatter。**経文本文・解説文本文はすべてここに置き、コード側に埋め込まない。**

### `content/sutra.txt`

全文のプレーンテキスト。**文字インデックスの唯一の基準**となるため、以下を厳守する。

- **改行は L0 の列の切れ目**として使う。1 行 = 1 列で、1 行は `COLS_PER_LINE = 16` 字以内に収める。
- 行の途中に空白・約物を入れない（ローダが除去するため `range` はずれないが、その升は空かずに詰まる）。
- 文字を追加・削除するとすべての `range` がずれるため、変更は `range` の一括見直しとセットで行う。
- 含まれる文字種は `assets/svg/` の 118 字に限る（ビルド時に検証）。

### `content/graph/*.yaml`

```yaml
id: goun                 # 一意のスラッグ（ローマ字）
kind: term               # sutra | phrase | term
label: 五蘊              # 表示ラベル（大書に使う文字列）
reading: ごうん
sanskrit:
  text: pañca-skandha    # ラテン翻字
  kana: パンチャ・スカンダ
summary: …               # 右中段の短い定義（数十字）
parent: shoken-to-kaiku
children: [shiki-rupa, ju, so, gyo, shiki-vijnana]
related: [juuhachi-kai]  # 隣接ノード（将来用）
layout: circle           # none | circle | column
range: [12, 17]          # 全文に対する文字インデックス範囲（半開区間 [start, end)）
audio: goun.m4a          # 読み上げ音源（assets/voice/ 配下のファイル名）
```

- `layout` は**ノード側が持つ**。円相か縦連結かは概念ごとに異なるため、描画側で決めない。
- `label` に含まれる各文字は `assets/svg/` に対応する SVG が存在しなければならない。
- `label` の**空白・改行は大書の列の切れ目**として読む（`headlineLayout`）。区切りを入れれば折り返し位置をコンテンツ側で決められ、入れなければ字数から自動で 1〜2 列に組む。字の大きさはいちばん長い列から決まる。列の切れ目は大書だけのもので、`range` との突き合わせ・グリフ在庫の検査・図の中の子の組版・現在位置インジケータは空白を除いた字だけを見る。

#### `id` の命名規約

`id` は一意のスラッグ（ローマ字・小文字・ハイフン区切り）で、**ファイル名は `<id>.yaml` / `<id>.md` と一致させる**（`content/graph/` と `content/docs/` の両方）。

`kind` ごとに次の形を取る。

| `kind` | 形 | 例 |
|---|---|---|
| `sutra` | 経の名 | `shingyo` |
| `phrase` | **`<start>-to-<end>`** | `kanjizai-to-issaikuyaku` |
| `term` | 語そのものの読み | `goun`・`shiki-rupa` |

`phrase` の `start` / `end` は、その句の**先頭の語**と**末尾の語**の読みをローマ字にしたもの。
句は数文字から数十文字まで長さがまちまちで、`label` 全体をスラッグにすると破綻するため、両端で挟んで示す。
`観自在菩薩……度一切苦厄` なら `kanjizai-to-issaikuyaku`。
両端の語は文法上の切れ目で取る（`観自在菩薩` の `菩薩`、`度一切苦厄` の `度` のような接辞は落としてよい）。
句が短く両端が同じ語になる場合は `-to-` を使わず、その語の読みだけを `id` にする。

#### `range` — 全文に対する文字インデックス範囲

- `content/sutra.txt` の先頭を `0` とする**半開区間 `[start, end)`**。`end - start` が文字数になる。
- `kind: phrase` は必須。`kind: term` も、全文中に対応する連続文字列があれば持つ（例: `五蘊` は句の中の 2 文字）。
- 全文中に現れない概念ノード（`前五識`・`阿頼耶識` など、経文に登場しない用語）は `range` を**持たない**。
  `range` の有無が「格子上にハイライトできるか」を決める。
- ルート（`kind: sutra`）は `range: [0, <全文長>]` とする。

ビルド時に次を検証する。

- `sutra.txt.slice(start, end) === label`（`range` を持つノードのみ。`label` の空白・改行は除いて比べる）
- 親が `range` を持つなら、子の `range` は親の範囲に**内包**されていること
- `range` が全文長を超えないこと

### `content/docs/*.md`

```markdown
---
id: goun
---

（長文解説の本文。DOM レイヤーで縦書き組版される）
```

### ビルド時検証（zod + 自作チェック）

CI で落とすべき条件：

- `parent` / `children` / `related` の参照切れ
- 親子関係の非対称（`a.children` に `b` があるのに `b.parent` が `a` でない）
- 到達不能な孤立ノード
- `id` に対応する `docs/*.md` の欠落
- `label` の構成文字に対応する SVG の欠落
- `layout: none` なのに `children` を持つ（またはその逆）

## ナビゲーション

### URL 同期

- `/#/goun/shiki` のようにノードパスを URL に載せる。初期はハッシュルーティングで十分。
- **ブラウザバック = ズームアウト** に割り当てる。共有リンクとしても機能する。

### 現在位置インジケータ（モックには無い追加要件）

- 画面端に **1 本の白い縦線**を引き、その横に先祖ノードを**縦に積み上げる**。
- 文字は**筆文字ではなく通常書体**（可読性優先）。大書との役割を明確に分ける。
- 各項目はクリック可能。押すとその階層まで一気にズームアウトする。
- 深さ = 潜水深度のメタファー。下端に薄い深度線を引くと『深般若』の名に合う。

### 左矢印

- モックの「左にコンテンツがある」印はそのまま採用。
- **PC・スマホ共通でドラッグによる左パン。** 操作を出し分けない（→ [画面外へはみ出してよい](#画面外へはみ出してよい)）。
- パン可能な方向を矢印の明滅で示す。左端に到達したら明滅を止める。
- 拡大中はドラッグが縦にも効く。矢印は左端の判定だけを見るので拡大しても意味は変わらない。
- L1 以降でも、本文が画面より広いときだけ出す。送り先が無い画面では出さない（示唆が嘘にならないようにする）。

### 音声

音は**ノイズにならないこと**を最優先の制約とする。存在を主張せず、環境として鳴っている状態を保つ。

#### BGM

- **1 トラックを最初から最後までループ再生し続ける。** 階層に応じたトラック切替やクロスフェードは**行わない**。
- 階層が変わっても音は途切れず、同じ場が続いている感覚を保つ。
- 自動再生制限があるため、**初回はユーザー操作をトリガに開始**する（最初のクリック／タップ）。
- ループ点は無音を挟まずシームレスに繋ぐ。継ぎ目でクリックノイズが出ないよう、ループ境界で数十 ms のクロスフェードをかける。
- 音量は控えめな既定値から始め、ユーザーが下げ切れる（ミュート可能な）UI を用意する。
- **上限ゲインを置く。** 音量 UI の 0〜1 はその範囲へ写し、スライダを振り切っても BGM が前に出ないようにする
  （現在は最大 1/4）。「ノイズにならない」制約を UI 操作で破れないようにするための天井である。

#### 配信方式（ストリーミングの可否）

**ストリーミングは可能。** `<audio>` 要素は HTTP Range によるプログレッシブダウンロードで再生でき、
`MediaElementAudioSourceNode` 経由で Web Audio グラフに載せられる（ダッキングやフィルタも効く）。
静的ホスティングのままで動作し、専用のストリーミングサーバは不要。

**ただし継ぎ目の問題がある。** 素朴に `<audio loop>` を使うと、MP3 / AAC が持つエンコーダ遅延とパディングにより
**ループ境界に無音の隙間が入る**。「ノイズにならない」という最優先制約に直接反するため、これは採れない。

サンプル精度でギャップレスにできるのは `decodeAudioData` + `AudioBufferSourceNode.loop` だが、これは全長デコードが前提。
参考として、現行の `sample_music_01.mp3`（7.7MB / 128kbps 換算で約 8 分）を全長デコードすると
float32 ステレオ 44.1kHz で **約 170MB** のメモリになり、モバイルでは通らない。

したがって方式は次の 2 案に絞られる。

| 案 | 方式 | ループ品質 | メモリ | 実装 |
|---|---|---|---|---|
| **A（推奨）** | ループ素材を 60〜120 秒に作り、Opus/WebM で 1〜2MB に収めて全長デコード。`AudioBufferSourceNode.loop = true` | **完全ギャップレス**（サンプル精度） | 120 秒で約 42MB。許容範囲 | 最も単純 |
| B | `<audio>` を 2 つ用意し、末尾数秒で交互にクロスフェード。ストリーミングのまま繋ぐ | 継ぎ目は隠れるが、同じ地点に戻る感覚は薄れる | 小 | 素直 |

- 案 A ならストリーミング自体が不要になる。瞑想 BGM は長尺の展開を要さないため、これが最も筋が通る。
- 長尺の素材を維持したい場合は案 B。
- MSE による自前チャンク供給も理論上可能だが、この用途には過剰。採らない。
- **どちらを採るかは未決**（→ [未決事項](#未決事項)）。素材の尺の決定と同時に確定させる。

#### 効果音

- ズーム遷移に `woosh` を重ねる。BGM より明確に小さく、単発で減衰しきる。
- 連続操作で重なって濁らないよう、同一 SFX の同時発音数を 1〜2 に制限する。

#### 読み上げ

- **収録音声を使う。** 制作者本人が読み上げて録音したファイルをノードごとに持つ。TTS は使わない。
- ファイルは `assets/voice/` に置き、YAML の `audio` フィールドで参照する。
- 「読み上げ」ボタン（右上）で再生。再生中はボタンが状態を示し、もう一度押すと停止する。
- 再生中は BGM を数 dB だけ絞る（ダッキング）。BGM を止めはしない。
- 音源が未収録のノードでは、ボタンを非表示にする（無効状態で置かない）。

#### 収録・エンコードの前提

| 項目 | 方針 |
|---|---|
| 収録 | 制作者本人。ノード 1 つ = ファイル 1 つ |
| 命名 | ノードの `id` と一致させる（`goun.m4a` 等）。YAML の `audio` で明示参照もできる |
| 形式 | 読み上げは AAC（`.m4a`）。BGM も配信用に圧縮する |
| 前処理 | 無音のトリム、ラウドネス正規化、ノイズ除去。読み上げ間で音量感を揃える |

## アセット在庫

| パス | 内容 | 状態 |
|---|---|---|
| `assets/svg/*.svg` | 筆文字グリフ **118 字**。`100x100` viewBox、単一 `<path>`、`fill="white"`、`clipPath` 付き | 揃っている |
| `assets/pattern/circle.svg` | **円相**（`layout: circle` の外周）。筆で描いた円のトレース | 揃っている |
| `assets/bgm/sample_music_01.mp3` | ループ BGM のサンプル（約 7.7MB） | 仮。ループ点の調整とファイルサイズ削減が必要 |
| `assets/sfx/woosh.wav` | 遷移効果音 | 仮 |
| `assets/voice/*.m4a` | 読み上げ音源（収録予定） | **未収録** |
| `sample_image/img_0{1,2,3}.png` | PC モック 3 枚 | 参照用 |

グリフ SVG の形式は全ファイルで統一されている（`width/height=100`、`viewBox="0 0 100 100"`、`<g clip-path>` + 単一 `<path fill="white">`）。
ビルドスクリプトはこの前提を検証してから読み込む。

### 円相（`assets/pattern/circle.svg`）の扱い

`layout: circle` の外周はこの素材を使う。手続き的生成はしない。形式がグリフ SVG と異なるため、専用の読み込み経路が必要。

| 項目 | 値 | 対応 |
|---|---|---|
| 生成元 | potrace 1.15 によるトレース | — |
| `viewBox` | `0 0 1273 1280` | ほぼ正方形だが**正確な正方形ではない**。アスペクトを保って配置する |
| `fill` | `#000000` | **黒で書かれている。** 描画時に `--ink` へ置換が必要 |
| `transform` | `translate(0,1280) scale(0.1,-0.1)` on `<g>` | Y 軸が反転している。パース時に適用を忘れると上下が逆になる |
| パス構成 | 主となる長大なパス 1 本 + 微小な断片パス **約 40 本** | 断片は筆のかすれ・飛沫。**間引かずに全て使う**（質感の源） |
| 形状 | 正円ではなく、線幅が場所により変化する閉領域（塗り） | ストロークではなく塗り図形として扱う |

- ビルド時に `scripts/build-glyphs.ts` と同じ経路でメッシュ化し、グリフと同様に TSL マテリアルを適用する。
- 円相も**粒子化の対象にする**と、円が散って再凝集する演出がそのまま得られる。断片パスが多いので粒子サンプル数は面積比で配分する。
- 出現アニメーションは、筆の運びに沿って現れる（円弧に沿った線形の露出）のが自然。パスを角度でソートしておくと実装しやすい。

## 初期スコープ

次の 1 経路のみを潜れれば足りる。

```
般若心経 全文
  └─ 観自在菩薩行深般若波羅蜜時 …        （句 / layout: none    / img_01 に相当）
       └─ 照見五蘊皆空                    （句 / layout: none）
            └─ 五蘊                       （語 / layout: circle  / img_03 に相当）
                 └─ 識                    （語 / layout: column  / img_02 に相当）
```

ノード数はルート + 4 + 五蘊の子 5 = 10。この規模でスキーマが痩せすぎ／太りすぎでないかを先に確かめる。

実装では **15 ノード**になった。`layout: column` には子が要るため、識の下に六識（眼識・耳識・鼻識・舌識・身識・意識）を置いたぶんが増えている
（→ [実装時に決めたこと](#実装時に決めたことreadme-に書かれていなかった判断)）。

### 完了条件

チェックは**画面で確認できたものだけ**に付ける。実装しただけで未確認のものは外したままにし、
その状態は [実装状況](#実装状況) に書く。

- [x] `L0` が 16 文字改行の格子として描画され、hover で一定範囲の文字だけが光る
- [ ] `L0` の紙面が左へはみ出し、ドラッグで辿れる（PC・スマホ共通の操作）
- [x] 潜る演出で、非持ち越し字が散開し、選んだノードの字が連続的に再配置される
- [x] 戻る演出で、現在字が再配置されつつ粒子がフェードインして凝集する
- [x] 上記 5 階層をズームイン／ズームアウトで往復できる
- [x] 3 種の `layout`（`none` / `circle` / `column`）がそれぞれ描画される
- [x] `circle` の外周に `assets/pattern/circle.svg` が使われている
- [ ] BGM が階層をまたいで途切れずループし続ける
- [ ] hover の琥珀グローが、文字のみ（circle）／文字 + 枠（column）で正しく出る
- [x] 現在位置インジケータが階層に追随し、クリックでズームアウトできる
- [x] ドラッグでのパンが WebGPU / DOM 両レイヤーで同期する（L1 以降で確認。L0 は確認環境で紙面が収まりパンが起きない）
- [x] URL が階層に同期し、ブラウザバックがズームアウトになる
- [ ] Tier 3（WebGL なし）で DOM レイヤーのみが成立する
- [ ] スマホ実機（最新 Chrome）で操作できる

## 実装状況

最終更新: 2026-08-08。確認環境は headless Chromium（WebGPU アダプタ無し → **Tier 2**、WebGL2 バックエンド）、1440x810
および 900x700（本文がはみ出す狭い画面）。
`npm run build`（グラフ検証 → グリフ前計算 → 型検査 → 本番ビルド）は通る。

### 完了している

| 領域 | 状態 |
|---|---|
| ビルド基盤 | Vite + TS + React 19 / r3f v9 / three 0.185。`WebGPURenderer` を async 初期化し、WebGPU が取れなければ WebGL2 バックエンドへ落ちる。**init 後に実バックエンドを見てティアを訂正する**（`navigator.gpu` があってもアダプタが無い環境があるため） |
| YAML | ビルド時に JSON へ変換する自作 Vite プラグイン。ランタイムに YAML パーサを載せない |
| グリフ前計算 | `scripts/build-glyphs.ts`。SVG パース → 平坦化 → 穴の判定 → 三角形分割 → 面積重み付きサンプリングまで自前（依存は three の `ShapeUtils` のみ）。118 字 + 円相 = 119 件、mesh 約 5.7MB / 粒子 49.2 万点。**ランタイムで SVG は一切触らない** |
| グロー用 SDF | `scripts/sdf.ts`。メッシュを高解像度の内外マスクへラスタライズし、8SSEDT で符号付き距離を出して 64×64 / 字の 8bit アトラス（1024×512、約 0.5MB）に焼く。発光の縁が字形をなぞるのはこれによる |
| 円相 | `Y` 反転・非正方形 viewBox・断片パス約 40 本を含めて取り込み済み。`fill="#000000"` は読まず、形状のみ取って描画時に `--ink` を当てる |
| コンテンツ | 15 ノードの YAML + 15 本の解説 Markdown。zod スキーマ + 静的検証（参照切れ・親子の非対称・孤立ノード・`range` の一致と内包・グリフ在庫・`docs` の欠落） |
| `L0` | 16 文字格子、字ごとの位相をずらしたゆらぎ、hover で句の範囲だけが琥珀に光り、周囲が沈む。範囲が列をまたぐ見え方も仕様どおり |
| `L1` 以降 | 3 種のレイアウト。`none` は 2 列折り返しの大書、`circle` は円相 + 円周配置、`column` は角丸矩形 + 箱のあいだだけを繋ぐ縦線 |
| DOM レイヤー | 縦書きの解説・サマリー（淡いオレンジの罫つき）・読み／梵語・現在位置インジケータ・読み上げボタン・左矢印。位置は `overlayInsets` の 3 つの目印から CSS が組む |
| ナビゲーション | 相を持つ FSM（`zooming-*` の間は入力を殺す）、ハッシュルーティング、ブラウザバック = ズームアウト |
| 遷移演出 | 持ち越しの字がメッシュのまま出発点から行き先へ移り、残りは粒子となって上へ流れて消える／凝集する。カメラも同じ時計・同じカーブで動く。L0 へ戻るときは字が凝集と同じ時間で立ち上がり、粒子は収まってから引く（→ [遷移演出](#遷移演出)） |
| 遷移中の引っかかり | 解消。紙面を畳まず、マテリアルも紙面で 1 本にして、面の用意を二度としない（→ [面の用意はやり直さない](#面の用意はやり直さない--演出が固まる原因)） |
| 墨とゆらぎ | 手続き的な墨のムラ（`inkDensity`）と、全深度で共通のゆらぎ（`sway.ts`）。どちらも粗さ・振れ幅を字の大きさに対して正規化してある |
| `L1` のカメラ | 大書が右へはみ出す画面では視野ごと右へ送る（`nodePanX`）。潜るあいだは等倍へ戻す |
| `L1` 以降のパン | 狭い画面で左へはみ出した本文を、ドラッグで左右にだけ辿れる。可動域は本文・サマリーの左端の実測から引き、ばねは atom 側に 1 つだけ置いて GPU レイヤーと DOM が同じ値を読む。左矢印は送り先があるときだけ出す |

### 実装済みだが未確認

画面で確かめられていないもの。**動く保証をしていない。**

| 項目 | 状況 |
|---|---|
| BGM のループ・ダッキング・SFX | Web Audio のグラフは組んである。headless では音を確認できない。継ぎ目の品質は実機で聴いて判断する |
| Tier 3 の DOM 紙面 | `PaperFallback` は書いてあるが、WebGL を落とした環境での確認ができていない |
| `L0` のドラッグパン | パン量を 1 つの atom に集約し、カメラと CSS 変数 `--pan-px` が同じ値を読む形になっている。L1 以降の同じ仕組みは画面で確認したが、**L0 は確認環境（16:9）では紙面が画面に収まってしまい、パンが発生しない**（→ 下の「可動域はビューポート依存」）。縦長の画面で見る必要がある |
| ホイール／ピンチでの拡大 | `zoomAtom` とカーソル固定の補正、拡大時の縦パンまで書いてある。ブラウザを持たない環境で作業したため**画面での確認ができていない**。特にトラックパッドのピンチ（`ctrlKey` 付きホイール）の効き具合と、2 本指の中点追従は実機で見て係数を詰める |
| `column` の hover グロー | 文字 + 枠を光らせる分岐は書いてあるが、hover した状態のスクリーンショットを撮れていない |
| hover のフェードと滲み | 発色・減光・グローを 0.4 秒（`FOCUS_FADE`）かけて渡す。滲みは SDF ベース（`createGlowMaterial`）で、届く距離は `GLOW_NEAR` / `GLOW_FAR`、濃さは呼び出し側（`L0` は `GLOW_STRENGTH`、大書は `GLOW_OPACITY`）。値は画面で見て詰める前提の初期値 |
| 読み上げ | 音源が未収録のため経路を通していない |

### 未着手・残作業

**[遷移演出](#遷移演出)は繋がった。** 持ち越しの字がメッシュのまま移動し、粒子も画面に出ている
（出なかった原因は `PointsNodeMaterial` の `sizeNode` が `Points` で読まれないことで、`Sprite` のインスタンシングへ移して解決）。
残るのは値の詰めと、確認できていない環境・経路。

- 粒子数・飛距離・尺は Tier 2 の画面で見て決めた初期値。**Tier 1（WebGPU 実機）での見え方は未確認。**
- スマホ実機、Tier 3、音まわり（下記）は依然として未確認。
- BGM 素材と読み上げ音源は未整備（→ [要対応（データ側）](#要対応データ側)）。

### 要対応（データ側）

| 項目 | 内容 |
|---|---|
| ~~`知.svg` が無い~~ | **解消済み。** `content/sutra.txt` の 208 文字目にあたる `知` を追加してもらった（`智` は在庫にあるが別字なので置換していなかった）。これで全 276 字が在庫内に収まり、`content:validate` はエラー 0 件になった |
| `舍` / `声` を正規化した | 在庫が `舎` / `聲` だったため `content/sutra.txt` を置換した。字数は変わらないので `range` はずれていない |
| ~~`content/sutra.txt` の空白~~ | **解消済み。** 改行を列の切れ目として扱うようにしたので、ファイルの行区切りと紙面の列が一致する（20 列）。行内に残る真言行の全角スペースだけはローダが除去し、`content:validate` が警告 1 件として知らせる |
| 読み上げ音源 | 未収録。ノードの `audio` は全て未設定で、ボタンは出ない |
| BGM 素材 | 8 分・7.7MB のまま。[配信方式](#配信方式ストリーミングの可否)が未決なので案 B を既定にしてある（下記） |

### 実装時に決めたこと（README に書かれていなかった判断）

後から読む人が「なぜそうなっているか」を追えるように残す。**設計を変えたつもりはなく、隙間を埋めた記録である。**

| 判断 | 理由 |
|---|---|
| **`layout: none` でも `phrase` は子を持ってよい** | `layout` が支配するのは「子の**関係図**」であって遷移そのものではない。[初期スコープ](#初期スコープ)の経路自体が句 → 句の下降を含むため、ここを CI で落とすと成立しない。検証は「`term` が `layout: none` で子を持つ」場合のみ落とす |
| **根（`kind: sutra`）は `range` と `label` を突き合わせない** | 根の `range` は全文を指すが、`label` は大書用の題名であって全文ではない |
| **識の子を六識（眼耳鼻舌身意）にした** | `layout: column` には子が要る。前五識・末那識・阿頼耶識は `assets/svg/` に字が無く大書できないため、在庫のある六識を採った |
| **可動域はビューポート依存にした** | 紙面は縦 16 升が画面高に収まるよう描かれるので、横がはみ出すかは画面のアスペクト比で決まる。**縦長のスマホでは必ずはみ出し、16:9 の PC では収まりきる。** 定数ではなくリサイズのたびに実測から引き直す |
| **拡大の下限は等倍** | 縮小まで許すと「縦 16 升が画面高に収まる」という紙面の前提が崩れ、可動域・行送り・字の大きさの根拠が全部揺れる。寄る方向だけを足した |
| **Tier 3 の紙面は拡大しない** | DOM の段組みなので `transform: scale` では字の詰まり方が破綻し、ワールド座標の対応も取れない。Tier 3 ではパンだけを許す（`usePanZoomGesture` の `allowZoom`） |
| **L1 以降のパンのばねは atom が持つ** | ドラッグは行き先だけを動かし、実際のパン量はその行き先へ向かって補間する。カメラ側でも補間すると 2 度掛かり、**大書だけが DOM の本文に遅れて 2 つの層がずれて見える**。カメラは atom の値をそのまま読む（`useNodePanSpring` / `CameraRig`） |
| **本文は切り詰めずに組み、はみ出しはパンで補う** | 幅で `max-width` を掛けると狭い画面では列が消え、本文が読めなくなっていた。列数は本文の量が決め、画面に入らないぶんはドラッグで送る（→ [画面外へはみ出してよい](#画面外へはみ出してよい)） |
| **パンの可動域は DOM の実測から引く** | 本文の列数は組版の結果なので、ワールド座標の計算では出せない。`offsetLeft`（`getBoundingClientRect` ではなく）で読む。パンは transform で当てるため、rect で測ると送った量が次の可動域に混ざる |
| **パンの捕捉はドラッグと判ってから** | `pointerdown` で即 `setPointerCapture` すると `pointerup` がキャンバスに届かず、クリックが成立しなくなる。6px 動いてから捕捉する |
| **hover の発光は `instanceColor` を使わない** | `WebGPURenderer` のノードマテリアル経路で期待どおりに効かなかった。インスタンス属性 + TSL で書き、Tier 1 / Tier 2 で同じ結果になるようにした |
| **当たり判定は `visible={false}` にしない** | 不可視のオブジェクトはレイキャストの対象から外れる。透明マテリアルで置く |
| **BGM は案 B を既定にした** | 現行素材が 8 分あり、案 A（全長デコード）はモバイルで通らない。1 定数（`BGM_STRATEGY`）で案 A へ切り替えられる形にしてある。**未決事項は未決のまま** |
| **`content/sutra.txt` は空白を除去して正規形にする** | ファイルを手で整形しても `range` がずれないようにするため。インデックスの基準は正規形 |
| **持ち越すのは「選んだ 1 ノードの字」だけにした** | 「生き残る文字」を範囲で決めると、潜り先で大書にならない字まで残ってしまう。深い側のノードに属する字を同じ順に 1 対 1 で対応づけ、数が合わなければ持ち越しを諦める（→ [持ち越される字](#持ち越される字--演出の軸)） |
| **粒子は `Points` ではなく `Sprite` のインスタンシング** | `PointsNodeMaterial` の `sizeNode` は `Points` に対して読まれず、粒が 1px に潰れて画面に出なかった。粒ごとの大きさが要るならこの経路しかない |
| **字と粒子で消える尺を別に持つ** | 揃えると字と粒子が同時に薄れ、「字が解けて粒になった」順序が出ない。字は 2 乗のカーブで素早く引き、粒子はそのあとも残って上へ流れる |
| **カメラを遷移と同じ時計・同じカーブで動かす** | spring の追従にすると、ワールド座標では直線に動く持ち越しの字が画面上で寄り道して見えた。`ease()`（smootherstep）を字とカメラで共有する |
| **ゆらぎの振れ幅は字の大きさに対する比** | 絶対値で持つと大書のような大きい字では揺れが相対的に消え、潜った先で字が固まって見える |
| **墨のムラの粗さはワールド単位で一定** | 字の大きさに比例させると、大書では筋が太くなりすぎて墨ではなく金属の光沢に見える |
| **`L1` 以降の寸法は `world/node-layout.ts` が単独で持つ** | 描画・遷移・DOM の 3 者が同じ値を引かないと、継ぎ目で字が跳ねオーバーレイが大書に重なる。DOM 側へは画面右端からの px として 3 つの目印を渡す |
| **横に狭い画面では視野ごと右へ送る** | 大書だけを左へ寄せると、大書と図の間隔が変わって「右から左へ流れる」構図が崩れる。カメラを送れば構図はそのまま保てる |
| **紙面が収まる画面では中央ぞろえ** | 送り先が無いのに右端ぞろえにすると左に大きな空白が空く。可動域が消える画面では中央に固定する |
| **起動直後の 1 フレームは補間しない** | 読み始めの位置へ補間で寄せると、紙面が中央から右へ流れて見える。初回はカメラごと飛ばし、窓の寸法から出した半幅で最初のフレームから正しい位置に描く |
| **BGM に上限ゲインを置いた** | 音量 UI を振り切っても BGM が前に出ないようにする。「ノイズにならない」制約を操作で破れなくするための天井 |

## ディレクトリ構成

```
src/
  scene/          # r3f: グリフメッシュ、粒子システム、TSL マテリアル、カメラリグ
    Stage.tsx       # Canvas + WebGPURenderer の初期化、ティア訂正
    Paper.tsx       # L0 の格子
    NodeStage.tsx   # L1 以降の大書 + 3 種のレイアウト
    Transition.tsx  # 遷移演出の指揮（持ち越し / 散開 / 凝集の振り分け、尺とカーブ）
    Particles.tsx   # TSL の粒子マテリアル（Sprite のインスタンシング）
    carry.ts        # いま持ち越されている字のノード id
    sway.ts         # 全深度で共通の字のゆらぎ
    glyphs.ts       # 前計算済みバイナリの読み込み
    tier.ts         # 性能ティア判定、フレームタイム計測
    materials.ts    # デザイントークンの GPU 側の写し、墨のムラ・滲み
  overlay/        # DOM: 縦書き解説、現在位置インジケータ、読み上げボタン、左矢印
    Overlay.tsx PaperFallback.tsx SpeakButton.tsx AudioControls.tsx LeftArrow.tsx
  nav/            # FSM、ルーティング、Jotai atoms
    fsm.ts atoms.ts router.ts
  content/        # YAML ローダ、zod スキーマ、グラフ型
    schema.ts loader.ts sutra.ts
  world/          # 両レイヤーが共有するワールド座標系
    paper.ts        # 格子の寸法と index → (column, row)
    pan.ts          # パン・拡大の可動域とドラッグ／ホイール／ピンチ操作
    node-layout.ts  # L1 以降の大書・図の寸法と、DOM 用の目印 overlayInsets
  audio/          # Web Audio: BGM ループ、クロスフェード、SFX、ダッキング
  generated/      # build-glyphs.ts の出力（グリフの索引）。コミットしない
scripts/
  build-glyphs.ts # SVG → メッシュ + 粒子サンプル + グロー用 SDF の前計算
  svg-path.ts     # 上記が使う SVG パーサ / 平坦化 / 穴の判定
  sdf.ts          # 上記が使う符号付き距離場の焼き込み（ラスタライズ + 8SSEDT）
  validate-graph.ts
content/
  sutra.txt       # 全文（文字インデックスの基準）
  graph/*.yaml
  docs/*.md
public/
  glyphs/         # build-glyphs.ts の出力（mesh.bin / particles.bin / sdf.bin）。コミットしない
assets/
  svg/            # 筆文字 118 字
  pattern/        # circle.svg（円相）
  bgm/ sfx/
  voice/          # 読み上げ収録音源
sample_image/     # PC モック（参照用）
prompts/          # 企画メモ
docs/             # 設計資料
```

### コマンド

| コマンド | 内容 |
|---|---|
| `npm run dev` | グリフを前計算してから開発サーバを起動 |
| `npm run build` | グラフ検証 → グリフ前計算 → 型検査 → 本番ビルド |
| `npm run content:validate` | `content/` の静的検証のみ |
| `npm run content:build` | グリフの前計算のみ |

## 未決事項

| 項目 | 論点 |
|---|---|
| BGM の配信方式 | [案 A（短尺ループ + 全長デコード）と案 B（2 要素クロスフェード）](#配信方式ストリーミングの可否)のどちらを採るか。素材の尺の決定と同時に確定する |
| 字の大きさの基準 | 「可読性から決める」の具体値。スマホでの 1 文字あたり最小 px を実機で決める必要がある |

### 設計対象外

| 項目 | 扱い |
|---|---|
| `related`（隣接ノード） | **今は設計しない。** YAML のフィールドとしては残すが、UI・遷移は定義しない |

### 決定済み（旧・未決事項）

| 項目 | 決定 |
|---|---|
| 全文レイアウト | **16 文字で改行し続ける格子**。意味による分節はしない → [ルート画面](#ルート画面全文格子) |
| 意味の区切り | `L0` では見せず、**`L1` から**現れる。フォーカス時は一定範囲の文字のみが光る |
| 句の範囲指定 | **文字インデックス範囲を YAML の `range` に持たせる**。基準は `content/sutra.txt` |
| 読み上げ音源 | **収録音声**（制作者本人が読み上げ）。TTS は使わない |
| BGM | **1 トラックを常時ループ再生**。階層クロスフェードなし。ノイズにならないことを最優先 |
| 円相の描画 | **`assets/pattern/circle.svg` を使用**。手続き的生成はしない |
| ルート格子の可視範囲 | **画面に収めない。PC・スマホ共通で左へはみ出す**。初期位置は紙面の右上 → [画面外へはみ出してよい](#画面外へはみ出してよい) |
| 深度をまたぐ演出 | **潜る＝他が散開して生存字が再配置／戻る＝現在字が再配置され粒子がフェードインして凝集**（非対称） → [遷移演出](#遷移演出) |
| `Tier 2` の演出 | 筋書きは全ティア共通。粒子数と粒度のみ落とす → [性能ティアごとの落とし所](#性能ティアごとの落とし所) |
| BGM のストリーミング | **技術的に可能**。ただしギャップレスループとの両立が要点 → [配信方式](#配信方式ストリーミングの可否) |

---

### 補足: 本ドキュメントの方針

経文本文および解説文の本文テキストは、意図的に本ドキュメントに含めていない。
これらは `content/graph/*.yaml` の `summary` および `content/docs/*.md` が唯一の出所（single source of truth）であり、
README・コード・コミットメッセージに複製しない。
